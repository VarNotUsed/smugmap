#!/usr/bin/env bash
# Demo: open a large SQLite database from S3 via smugmap LD_PRELOAD.
# The database is never fully downloaded — only the pages SQLite touches
# are fetched on demand via userfaultfd range requests.
echo "sqlite.sh demo: not yet implemented"
