#!/usr/bin/env bash
# Demo: run llama.cpp against a large GGUF model file stored on S3 via smugmap LD_PRELOAD.
# Only the model layers actually accessed during inference are fetched on demand.
echo "llama.sh demo: not yet implemented"
